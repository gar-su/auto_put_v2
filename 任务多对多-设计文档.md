# 任务多对多-设计文档

## 一、背景与目标
现状：一个任务只能投一个媒体、一个账户包、一个投放策略包、一个短剧包、一个素材包。运营若要同时投 Meta 与 TikTok，或对同一批素材做 A/B 对比，只能建多个任务，把重复的配置抄多遍。
问题一：媒体开关（`#taskMediaBtns`）本身不提交，只反向过滤「投放策略包 / 账户包」两个下拉，任务真正只存 `mediaPkgId`，媒体由包派生。一旦任务要引用多个投放策略包，媒体就成了悬空概念，无法回答「这个任务投了哪些媒体」。
问题二：短剧包与素材包是媒体无关的包，媒体差异被封闭在投放策略包与账户包内。任务侧仍是单引用，包的可复用性被任务的单引用上限卡死。
问题三：任务列表的媒体列、包引用列都是单值展示，多引用后无法表达。
目标：任务支持引用多个媒体投放组合，展开成多个互相独立的执行单元，各自独立建系列。
范围：仅 Meta 端与 TikTok 端共用一套结构。跨平台字段差异已由投放策略包内的媒体×目标注册表承担，本次不改其内容。

---
## 二、核心模型：媒体行与执行单元
任务不再有「媒体」开关。媒体是媒体行的属性，由运营显式选出。
```
任务
 ├─ 媒体行（多行，每行 = 一个媒体 + 恰好一对 账户包 / 投放策略包）
 ├─ 短剧包（多选，媒体无关）
 ├─ 素材包（多选，媒体无关）
 └─ 运行约束（任务级一份）
```
执行单元由媒体行与两个媒体无关包叉乘得出：
```
执行单元 = 媒体行 × 短剧包 × 素材包
```
媒体行与执行单元是一一对应的：每个媒体行恰好产生一个执行单元，因此「多对多」只发生在短剧包与素材包两个维度上。媒体维度只是把执行单元分成若干组，每组落自己的账户与投放配置。
每个执行单元独立建系列，互不干扰。一个单元失败不影响其余单元。

---
## 三、任务表单改造

### 1. 表单结构
```
基本信息：任务名称 · 状态 · 生效时间段 · 执行频率
执行单元：媒体行列表（媒体 · 账户包 · 投放策略包 · 移除），底部「添加媒体行」
规则引用：短剧包（多选）· 素材包（多选）
运行约束：结束运行条件 · 单轮单短剧计划创建上限 · 每轮账户数
```
「投放媒体」区块整体替换为「执行单元」区块，原媒体开关按钮组 `#taskMediaBtns` 移除。

### 2. 媒体行
| 项 | 说明 |
|------|------|
| 媒体 | 下拉，首行为空，选项为 Meta / TikTok |
| 账户包 | 下拉，选项按本行媒体过滤，已停用与已删除的不出现 |
| 投放策略包 | 下拉，选项按本行媒体过滤，已停用与已删除的不出现 |
| 移除 | 仅当行数大于 1 时可点 |
| 添加媒体行 | 列表底部，新增一行空行 |
媒体选定后才填充该行的两个包下拉，媒体未选时两者置灰。
下拉旁保留「查看」，打开只读摘要抽屉。摘要按本行当前选中的包打开，不跳页。

### 3. 执行单元计数提示
媒体行列表下方实时显示：`当前共 N 个执行单元（M 个媒体 × K 个短剧包 × L 个素材包）`。
短剧包或素材包未选时该提示显示为「请先选择短剧包与素材包」。
单媒体行、单短剧包、单素材包时行为与改造前完全一致，运营无感知。

### 4. 多选控件的呈现
短剧包与素材包由单选改为多选，均采用带复选框的下拉：收起时显示 `已选 N 项`，展开为可勾选列表，已选项的包名在输入框下方以标签呈现，可逐个移除。
已停用与已删除的包不出现在候选中；已选中的包若被停用，保留选中态并在标签上标黄，任务保存时按失效态处理。

---
## 四、执行单元与运行时

### 1. 单元独立性
每个执行单元持有自己的媒体、账户包、投放策略包，以及从任务继承的同一份短剧包与素材包。
单元之间的建系列过程无顺序依赖，可并发执行。

### 2. 共享配置的作用域
| 配置 | 作用域 | 说明 |
|------|------|------|
| 单轮投放次数 | 任务级 | 全任务共有一个轮次计数，不是每个单元各跑一遍 |
| 累计投放次数 | 任务级 | 同上，与单轮次数共同决定任务总轮数 |
| 广告数 | 任务级 | 单个系列的广告数，各单元一致 |
| 结束运行条件 | 任务级 | 指标按账户维度统计，见第五章 |
| 单轮单短剧计划创建上限 | 任务级 | 每个执行单元对本单元命中的短剧各自套用 |
| 每轮账户数 | 任务级 | 每个执行单元对本单元账户包内账户各自套用 |
任务总轮数 = 单轮投放次数 × 累计投放次数。各执行单元在这同一个总轮数内分头建系列，不各自乘一遍。

### 3. 账户重叠
同一账户允许被多个媒体行引用，不做前置拦截。
账户重叠时不由前端仲裁，由后台在运行时去重，保证同一账户不重复建系列。
现有的素材×短剧维度去重不覆盖账户维度，账户维度的仲裁为本次新增的运行时机制。

---
## 五、冲突处理

### 1. 冲突清单
| # | 冲突 | 机制 | 处理方式 |
|------|------|------|------|
| 1 | 任务级「媒体」与「投放策略包」的媒体属性打架 | 媒体行承载媒体，两个包下拉按行媒体过滤 | 前置消解，结构上不可选错 |
| 2 | 同一账户被多个媒体行引用 | 允许重叠 | 运行时去重，不做前置拦截 |
| 3 | 同一短剧被多个媒体投 | 短剧包按语种分行已支持 `{Meta, TikTok}` 双值 | 无新增冲突，保持现状 |
| 4 | 定向包的媒体归属 | 定向包是媒体级的，短剧包的语种行同时容纳两个媒体 | 按媒体校验归属，见下节 |
| 5 | 指定素材与规则圈剧 | 包级约束 | 沿用原校验，不随单元数变化 |
| 6 | 结束运行条件的账户维度统计 | 指标按账户算，重叠账户被两个单元各读一半 | 与冲突 2 同源，归入运行时仲裁 |

### 2. 定向包的媒体归属校验
短剧包的语种行同时容纳 Meta 与 TikTok 两种定向包，该结构不变。
任务保存时，按本任务实际选中的媒体集合校验：媒体行覆盖到的每个媒体，在短剧包的每个语种行上都必须有定向包落位；某媒体在某语种行未落位，表示该媒体不投该语种，不视为错误。
定向包的引用计数与删除拦截由短剧包的定向引用判定统一承担，改为按媒体分组统计，列表展示时区分 `Meta: 2 / TikTok: 1`。

### 3. 不做的取舍
不把引用基数从「媒体行 × 短剧包 × 素材包」收敛为更小的组合。运行约束、素材配置、结束条件全部保留在任务级，不下放到媒体行，避免表单复杂度翻倍。
不为媒体行单独维护一套定向包筛选。定向包按语种分行已表达媒体归属，再叠一层行级筛选是重复建模。

---
## 六、任务列表与引用关系

### 1. 任务列表
| 列 | 改造 |
|------|------|
| 媒体 | 由单值改为多值，展示本任务全部媒体行的媒体，如 `Meta / TikTok` |
| 竞价策略 | 多媒体行时取首个媒体行的投放策略包 |
| 日预算 | 多媒体行时取首个媒体行的投放策略包 |
| 优化目标 | 同上 |
| 成效目标 | 同上 |
| 渠道号 | 同上 |
| 短剧包 | 多值时展示 `简名 + (N)`，点开只读清单，可跳转各包详情 |
| 素材包 | 同上 |
| 投放策略包 | 多值时展示 `首个包名 + (N)`，点开只读清单 |
| 账户包 | 同上 |
多值列的完整清单在只读摘要抽屉内逐项列出，列表只展示压缩形态。

### 2. 执行单元子行
执行单元在任务列表中展开为子行，默认收起，任务行首列提供展开箭头，行上显示本任务的单元数。
每个子行展示该单元的媒体、账户包、短剧包、素材包、本轮已建系列数、状态与失败原因。
借列表原有的行内操作槽位承载展开箭头，不新增列。

### 3. 执行单元的状态
| 状态 | 含义 |
|------|------|
| 待执行 | 本轮尚未开始 |
| 执行中 | 正在建系列 |
| 已完成 | 本轮正常结束 |
| 已跳过 | 引用的包被停用，本轮未执行 |
| 失败 | 执行中出错，展示失败原因 |
任务行状态由下属单元归并：任一单元失败则任务标记为异常，其余归并规则在实现时按后台返回口径对齐。

### 4. 失效态
任务引用的任一包被停用，任务行仍按原有规则标黄，悬浮提示中列出全部失效包。
执行单元子行单独标记出由哪个失效包导致跳过。

### 5. 复制任务
复制任务即复制全部引用，新任务指向同一批包，不复制包内容。媒体行、短剧包多选、素材包多选整组复制。

### 6. 引用反查
包管理页的「引用任务数」列统计引用该包的任务数，不按执行单元数重复计数。
只读摘要抽屉中的引用清单同样按任务去重。

---
## 七、校验

### 1. 任务保存校验
| # | 校验 | 提示 |
|------|------|------|
| 1 | 任务名称必填 | 请输入任务名称 |
| 2 | 执行频率必填 | 请输入执行频率 |
| 3 | 至少一个完整媒体行（媒体、账户包、投放策略包三者齐全） | 请至少填写一个完整的执行单元 |
| 4 | 媒体行内媒体不可重复 | 同一个媒体只能配置一行 |
| 5 | 短剧包至少选一个 | 请选择短剧包 |
| 6 | 素材包至少选一个 | 请选择素材包 |
| 7 | 引用的包不得为停用或已删除 | 引用的 XX 包已停用，执行时将告警并跳过本轮 |
| 8 | 素材包中存在指定素材模式时，短剧包中必须存在手选清单模式 | 指定素材需配合手选短剧清单使用 |
| 9 | 账户包内账户数非空 | 当前账户包内无账户 |
| 10 | 每轮账户数不得超过所在账户包内账户数 | 每轮账户数不能超过包内账户数 |
| 11 | 结束运行条件数值必填 | 请填写结束运行条件的数值 |
| 12 | 单轮单短剧计划创建上限小于 1 且非空时拦截 | 计划创建上限不能小于 1（不填=不限） |
| 13 | 每轮账户数小于 1 且非空时拦截 | 每轮账户数不能小于 1 |
第 8 条为包级存在性校验，与媒体行数量无关，不随执行单元数变化。
第 11 至 13 条为本次补齐（此前只校验了任务名称与执行频率）。

### 2. 包管理页的连带校验
- 被任一任务引用的包不可删除，提示被 N 个任务引用。
- 被短剧包引用的定向包不可删除，引用计数按媒体分组统计。
- 包停用后不可被新任务选择，引用它的既有任务执行时告警并跳过本轮。

---
## 八、风险标注
### 1. 账户维度去重是新增机制
现有去重是素材×短剧维度，不覆盖账户维度。允许账户重叠后，并发执行的两个单元可能同时读写同一账户的在投系列数，导致超出结束运行条件的上限。
该仲裁必须在后台以跨执行单元的原子操作实现，前端无法承担。这是本次改造唯一需要在后台新建的机制。

### 2. 结束运行条件的统计口径
结束运行条件的指标为「单账户单短剧在投总系列数」，按账户统计。账户被多个单元共用时，该指标在单元之间是共享量，统计口径需在后台明确为跨单元汇总，否则各单元各自判断会提前或延后触发。

### 3. 笛卡尔积的规模
执行单元数 = 媒体行数 × 短剧包数 × 素材包数。三个维度各选 3 项即产生 27 个单元，运营容易在无感知的情况下放大投放规模。
单元计数提示常驻表单，并在保存前二次确认实际单元数。

---
## 九、验收标准
| # | 验收项 |
|------|------|
| 1 | 任务表单的「投放媒体」区块替换为「执行单元」区块，原媒体开关按钮组移除 |
| 2 | 媒体行可增删，首行不可删；每行的账户包与投放策略包下拉按本行媒体过滤 |
| 3 | 媒体系与任一下拉未选时，该行不参与执行单元计数 |
| 4 | 媒体行列表下方实时显示执行单元总数，并给出媒体数 × 短剧包数 × 素材包数的拆解 |
| 5 | 短剧包与素材包为多选，收起态显示已选数量，已选项以标签呈现且可逐个移除 |
| 6 | 单媒体行、单短剧包、单素材包时，表单行为与改造前一致 |
| 7 | 任务保存产出媒体行数组、短剧包数组、素材包数组，不再产出单个 mediaPkgId |
| 8 | 媒体行内媒体重复、行不完整、包停用、账户包为空、数值非法均被拦截并给出对应提示 |
| 9 | 指定素材需配合手选短剧清单的校验仍在，且与媒体行数量无关 |
| 10 | 结束运行条件、单轮单短剧计划创建上限、每轮账户数在任务级唯一，不随媒体行复制 |
| 11 | 每个执行单元独立建系列，单元之间无执行顺序依赖 |
| 12 | 同一账户被多个媒体行引用时任务可保存，由后台运行时去重 |
| 13 | 任务列表媒体列展示全部媒体，包引用列多值时展示压缩形态并可点开只读清单 |
| 14 | 任务行可展开为执行单元子行，子行展示单元自己的账户包、短剧包、素材包与状态 |
| 15 | 执行单元状态含待执行、执行中、已完成、已跳过、失败五种，失败展示原因 |
| 16 | 任一单元失败时任务行标记为异常 |
| 17 | 复制任务整组复制媒体行与多选包，指向同一批包 |
| 18 | 包管理页引用任务数按任务去重，不按执行单元重复计数 |
| 19 | 定向包引用计数按媒体分组统计，列表区分展示各媒体的引用数 |
| 20 | 定向包表单与短剧包语种行的结构不变，仍按语种同时容纳 Meta 与 TikTok |

---
## 十、待议
### 1. 任务行状态的归并规则
任一单元失败则任务标记为异常已定，但「部分成功」是否需要独立状态、其余归并口径如何，需后台在执行单元状态返回后确认。
### 2. 执行单元的历史留存
执行单元子行展示的是本轮还是最近一轮，跨轮次的历史是否保留，取决于后台是否落库单元级的执行记录。
### 3. 单元计数上限
是否需要为执行单元总数设置硬上限（如超过 50 个拦截），防止运营误操作放大投放规模，待运营侧确认。
### 4. 竞价策略等列表列的取值
多媒体行时列表列取首个媒体行的投放策略包，是否为运营期望的展示口径，或改为多值压缩展示，待确认。