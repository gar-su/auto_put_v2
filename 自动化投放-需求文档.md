# 自动化投放-需求文档
## 一、范围与定位
本文档是项目唯一的权威需求文档。业务口径以 `auto_put`（Meta 线上）与 `auto_put_tiktok`（TikTok 线上）为准，交互形态以 `auto_put_v2/index.html` 原型为准。执行链路的后台业务逻辑以 `MetaAutoTask-素材逻辑.md` 为准。
---
## 二、执行任务逻辑
本章描述一个任务从被调度到落库建出广告系列的完整过程。

### 1. 调度与并发
系统按固定间隔轮询所有启用中的任务，逐个判断本轮是否可以执行。判断依据两条，须同时满足：
| 条件 | 说明 |
|------|------|
| 生效时间段 | 当前时间落在任务配置的任一时间段内。时间段为开区间，整点边界不命中 |
| 执行频率 | 距上次执行完成已超过配置的间隔分钟数。从未执行过、或未配置间隔的，直接放行 |

并发控制分两层：同一创建人的任务串行执行；同一创建人的任务列表内最多并行若干个任务，每个任务再单独加锁。执行前会再读一次任务状态，防止多实例重复执行。

### 2. 一轮任务的完整链路
```
拉取启用中的任务 → 判断本轮可跑 → 按创建人分组加锁 → 逐任务执行
                                                      ↓
                                            读取账户包 → 取短剧列表
                                                      ↓
                                            按短剧逐部循环
                                                      ↓
                                    解析定向 → 捞素材池 → 算系列上限
                                                      ↓
                                        按账户逐个建系列 → 落库 → 通知下游上传
```
每一轮结束会写一条任务运行记录，并更新任务的「上次完成时间」。

### 3. 短剧运行条件：决定投哪些剧
任务的短剧来源分两种圈选方式：
| 方式 | 行为 |
|------|------|
| 按规则 | 按语种、指标筛选、上线时间、初次投放时间等维度实时算出命中集，不在任务上固化。命中集随新剧上线自动变化 |
| 手选清单 | 保存任务时必须选出具体短剧列表，命中集固定 |
按规则时**仅允许筛选最近 30 天内有消耗的短剧**，更早的短剧不在候选范围内。
取到短剧列表后按部循环。**任务是一部剧一部剧跑的**：账户包整份任务共用，但素材池、已用素材、每个账户已建系列数，都按「任务 + 这一部剧」分开记账。A 剧用过的素材不会算作 B 剧也用过。
每部剧带自己的定向包。**该剧没有可用定向时，这部剧整部跳过，不建系列，不影响其他剧。**

### 4. 素材规则：决定这部剧拿哪些片子
素材永远跟在短剧后面：先有剧，再按这部剧找素材，不是先捞全库再往剧上贴。
两种选法：
| 选法 | 行为 |
|------|------|
| 按规则筛选 | 在该剧的素材库中按条件捞取。可选素材创建时间、数据门槛（消耗、ROI、达标率等）、业务类型、媒体、统计窗口、排序方式、数量上限 |
| 指定素材 | 手工勾选。系统只使用勾选时绑定的短剧 ID 等于当前正在跑的这部剧的那些素材 |
按规则筛选时，任务创建人必须能看到素材所在文件夹，看不到的不进池子。数量不限时最多取 1000 条，选固定数量则先随机打乱再截取。
素材规则是任务级一份，但**对每部剧会单独查一遍**。同一套「消耗 ≥ 100」在剧 A 可能有 80 条，在剧 B 可能 0 条。

### 5. 素材与短剧的对应关系
| 场景 | 结果 |
|------|------|
| 剧在范围内，规则能筛出素材 | 这部剧进入建系列 |
| 剧在范围内，但规则太严 / 文件夹没权限 / 指定素材没挂这部剧 | 记「没有素材」，跳过这部剧，不影响其他剧 |
| 开启跨轮去重，且该剧素材上一轮都用过 | 池子被剔空，同样视为没有可用素材 |
| 有素材，但条数不够「每个系列要绑几条素材」 | 凑不出完整一组，一个系列都不建 |
跨轮去重由任务上的开关控制。开启后，已投放过的素材记在该剧名下，下一轮不再发放；关闭后仍会在本轮内避免同一条素材进两个系列。

### 6. 执行单元：任务如何展开
一个任务由以下维度组成：
```
执行单元 = 媒体行 × 短剧包 × 素材包
```
| 维度 | 说明 |
|------|------|
| 媒体行 | 每行 = 一个媒体 + 恰好一对「账户包 / 投放策略包」。媒体行的媒体不重复 |
| 短剧包 | 可多选，媒体无关 |
| 素材包 | 可多选，媒体无关 |
媒体行与执行单元一一对应：每个媒体行恰好产生一个执行单元，因此多对多只发生在短剧包与素材包两个维度上。
**每个执行单元独立建系列，互不干扰。一个单元失败不影响其余单元。**

### 7. 去重规则
同一媒体 + 同一账户包下，被多个包命中的短剧或素材只进池一次，配置以先选中的包为准。
不同媒体各自建池，互不去重——Meta 与 TikTok 投同一部剧是正常的，不是重复。
去重发生在构建候选集时，不等于一定建出系列。是否真的建出，还取决于素材够不够一组、账户有没有满、本轮上限有没有到。

### 8. 账户与上限
三层配置，作用域不同，容易混，分开记。
| 配置 | 作用 | 作用域 |
|------|------|------|
| 账户包 | 候选人名单。包内有哪些账户，这部剧就只在这些账户上建系列 | 任务级，每部剧共用同一份 |
| 单轮单短剧计划创建上限 | 本轮给这部剧最多新建几个系列 | 任务 + 剧 |
| 每轮账户数 | 本轮最多允许几个账户参与建系列，控制单轮起量规模 | 每个执行单元各自适用，取各自账户包的账户 |
没有账户包，整轮任务起不来。

### 9. 系列数怎么算出来
「打算建几个」不等于「建得出来」。实际系列数取三者中最小：
- 单轮单短剧计划创建上限
- 可用素材条数 ÷ 每个系列要绑的素材数（整除，余数作废）
- 每轮账户数覆盖到的、尚未满员的账户个数
举例：上限填 5，每系列 30 条素材，该剧筛出 50 条 → 50÷30=1，最多建 1 个系列，不是 5。筛出 20 条 → 凑不满一组，一个都不建。

### 10. 停止条件
结束运行条件由两项组成，配合卡住整部剧的建系列：
| 配置 | 含义 |
|------|------|
| 单账户在投系列数 | 对「任务 + 剧 + 账户」已建过的系列数。达到阈值，该账户本轮视为满员，跳过，不再给它建 |
| 账户数 | 有多少个账户已经达到上面的满员。达到这个个数，这部剧整段停建，本轮后面账户也不再建 |
计数按「任务 + 短剧 + 账户」分开。剧 A 的账户 1 满了，不影响剧 B 的账户 1。
计数在**落库时就加一，不等广告系列在媒体后台创建成功**。上传失败时该计数仍可能已经加上，与后台真实在投数可能短暂不一致。

### 11. 素材与账户抢资源时谁说了算
同一轮里素材是发完就没的：账户 1 拿走一组建成系列，账户 2 再拿一组；牌不够下一组时，即使后面账户还没满、本轮上限也还没到，也会停。
因此会出现「结束条件还没达标，但日志说没有素材」——不是账户配错了，是该剧可用素材不够再开一组。开启跨轮去重后更明显：上一轮把池子用光，下一轮该剧会持续没有素材。

### 12. 落库与下游
建系列时在同一事务内写入广告系列、广告组、广告与素材关联。广告组固定一条，一条广告对应一条素材。落库完成后通知下游执行上传，素材文件另行调度上传。
---
## 三、页面结构
### 1. 外层导航
外层导航分两组，切换时全局清空当前页面再激活目标页，面包屑同步重写。
| 分组 | 页面 |
|------|------|
| 自动化任务 | 任务、广告系列、广告组、广告 |
| 前置配置 | 账户包、投放策略包、定向包、短剧包、素材包 |
两组各自维护激活态。运行约束不单独成页，已并入任务表单。

### 2. 任务页
| 项 | 内容 |
|------|------|
| 筛选栏 | 任务ID、任务名称、任务状态、竞价策略、创建人、创建时间区间、更新时间区间；竞价策略为单次成效费用目标 / 竞价上限 / 广告花费回报目标时，追加对应的参数输入项 |
| 操作 | 查询、重置、新建任务 |
| 批量条 | 已选任务数、批量启动、批量暂停 |
| 概览卡 | 已启用任务数、广告计划上传成功率、广告组上传成功率、广告上传成功率 |
| 表格列 | ID、任务名称、状态、媒体、执行间隔、上次完成时间、创建人、更新人、创建时间、更新时间、短剧包、素材包、投放策略包、账户包、操作 |
| 行内操作 | 编辑、复制、日志 |
任务名称列可点开编辑弹窗；引用的包失效时标黄并悬浮提示。
四个包引用列展示该任务引用的全部包名，单行显示、超出截断，悬浮显示完整清单；点击打开包引用清单抽屉，逐项列出并可跳转到单个包详情。任一引用包失效时该格标红。
媒体列在媒体行多于一行时提供展开入口，展开后插入执行单元子行。子行的列位与父行对齐，四个包落在与父行相同的列，其中投放策略包与账户包合并为一格，按「账户包 / 投放策略包」成对展示，还原「一个媒体行一对包」的绑定关系。子行另展示该单元的已建系列数与失败原因。执行单元状态含待执行、执行中、已完成、已跳过、失败五种。

### 3. 广告系列页
| 项 | 内容 |
|------|------|
| 筛选栏 | 创建时间区间、任务ID、任务名称、上传广告系列ID、账户名称、系列名称、渠道号、监测链接ID或名称 |
| 表格列 | ID、任务ID、广告系列ID、广告系列名称、账户名称、账户ID、剧本编号、剧本名称、短剧名称、渠道名称、渠道号、上传状态、系列状态、竞价策略、日预算、优化目标、失败原因、链接名称、回传模板、创建时间、更新时间、操作 |
| 行内操作 | 重试 |
上传状态与系列状态分列展示。监测链接支持批量导入。

### 4. 广告组页
| 项 | 内容 |
|------|------|
| 筛选栏 | 创建时间区间、任务ID、任务名称、账户名称、上传系列ID、系列名称、上传广告组ID、广告组名称、渠道号 |
| 表格列 | ID、任务ID、上传系列ID、广告组名称、账户信息、投放短剧、上传状态、广告组状态、定向包、失败原因、优化目标、创建时间、更新时间、操作 |
| 行内操作 | 重试 |

### 5. 广告页
| 项 | 内容 |
|------|------|
| 筛选栏 | 创建时间区间、任务ID、账户名称、上传广告组ID、广告组ID |
| 表格列 | ID、任务ID、广告系列ID、广告组ID、上传广告组ID、广告ID、账户名称、账户ID、上传状态、广告状态、多广告主、失败原因、素材名称、创建时间、更新时间、操作 |
| 行内操作 | 重试 |

### 6. 包管理页（五页同构）
五个包管理页结构同构：筛选栏 + 表格 + 抽屉表单。
| 页 | 筛选栏 | 表格列 |
|------|------|------|
| 账户包 | 账户包名称 | ID、账户包名称、媒体、状态、账户数、引用任务数、创建时间、更新时间、操作 |
| 投放策略包 | 投放策略包名称 | ID、投放策略包名称、媒体、状态、规则摘要、引用任务数、创建时间、更新时间、操作 |
| 定向包 | 定向包名称 | ID、定向包名称、媒体、定向条件、引用短剧包、创建时间、更新时间、操作 |
| 短剧包 | 短剧包名称 | ID、短剧包名称、状态、规则摘要、引用任务数、创建时间、更新时间、操作 |
| 素材包 | 素材包名称 | ID、素材包名称、状态、规则摘要、引用任务数、创建时间、更新时间、操作 |
行内操作为编辑、复制、停用、删除。引用任务数可点开查看引用该包的任务清单。

### 7. 弹窗与抽屉清单
| 名称 | 归属 | 用途 |
|------|------|------|
| 任务表单 | 任务页 | 新建 / 编辑任务 |
| 账户包抽屉 | 账户包页 | 新增 / 编辑账户包，含账户名单选择 |
| 包抽屉 | 投放策略包 / 短剧包 / 素材包页 | 新增 / 编辑对应包 |
| 定向包抽屉 | 定向包页 | 新增 / 编辑定向包 |
| 包只读摘要抽屉 | 任务表单、任务列表、包引用清单 | 查看被引用包的关键配置，不跳页 |
| 包引用清单抽屉 | 任务列表的包引用列 | 列出该任务引用的该类型全部包，可跳转到单个包详情 |
| 定向包选择抽屉 | 任务表单 | 为短剧选择定向包 |
| 商品对齐抽屉 | 投放策略包 | TikTok 商品数据源开启后，按账户与商品库对齐商品 |
| 素材选择弹窗 | 素材包 | 指定素材模式下勾选素材 |
| 引用任务清单 | 包管理页 | 查看引用了该包的任务 |
| 批量导入监测链接 | 广告系列页 | 批量录入监测链接 |
---
## 四、四个包
| 包 | 媒体属性 | 承载内容 |
|------|------|------|
| 账户包 | 有 | 名称、媒体、账户名单、收益方与赞助方 |
| 投放策略包 | 有 | 广告目标及其驱动的全部投放配置、追踪配置 |
| 短剧包 | 无 | 圈剧规则或手选清单、定向包分配、排期 |
| 素材包 | 无 | 素材选择方式、筛选条件、素材数量、投放次数、广告数、指定素材清单 |
定向包不属于上述四包，它有独立的前置配置页与独立抽屉，由短剧包引用。

### 1. 引用规则
任务只存包引用，不存字段值。改包保存后不回写任何已建任务，任务下一次执行时读取最新配置。正在执行中的那一轮使用启动时的快照跑完，不中途更换规则。已建出的广告系列不回滚。
引用只允许一层：短剧包可引用定向包，包不能引用包。
被任务引用的包不可删除；被短剧包引用的定向包不可删除。包停用后不可被新任务选择，引用它的既有任务执行时告警并跳过本轮。

### 2. 账户包
| 字段 | 必填 | 说明 |
|------|------|------|
| 账号包名称 | 是 | 最长 50 字 |
| 媒体 | 是 | 决定账户候选范围 |
| 收益方与赞助方 | 否 | 可添加多行 |
| 账户名单 | 是 | 勾选入包的账户 |
账户名单表格支持翻页、每页条数、全选当前页、按账户 ID 与按账户名分别搜索。已选账户以标签区呈现。

### 3. 投放策略包
包表单由**媒体**与**广告目标**两个维度决定。媒体在包上显式选择，广告目标是包内第一个字段。
| 媒体 | 广告目标 |
|------|------|
| Meta | 应用推广 / 销量 |
| TikTok | 应用推广 / 销量 |
切换任一维度，表单字段整组替换。表单按投放形态、广告系列设置、广告组设置、广告设置四个分组呈现。

#### Meta / 应用推广
| 分组 | 字段 |
|------|------|
| 投放形态 | 业务类型、渠道、投放策略（固定直投）、回传模板 |
| 广告系列设置 | 单日预算额度、广告系列竞价策略、成效目标、转化事件、归因设置、单次成效费用目标、出价金额、广告系列状态 |
| 广告组设置 | 移动应用商店、应用、应用链接、定向包 |
| 广告设置 | 计费方式、多广告主广告、行动号召、深度链接、使用进阶赋能型素材 |
联动：成效目标候选随广告系列竞价策略收窄；单次成效费用目标仅在竞价策略为单次成效费用目标时出现；出价金额仅在竞价策略为竞价上限或广告花费回报目标时出现；转化事件仅在成效目标为转化价值最大化、或竞价策略为单次成效费用目标时出现；应用候选随移动应用商店切换。

#### Meta / 销量
投放形态改为业务类型、渠道、投放策略（W2A / H5）、选择 H5、回传模板、像素 ID。选择 H5 仅在投放策略为 H5 时出现。广告系列设置与广告设置同应用推广。广告组设置仅定向包一项。

#### TikTok / 应用推广
| 分组 | 字段 |
|------|------|
| 投放形态 | 业务类型、渠道、投放策略（固定直投）、选择应用、回传模板 |
| 广告系列设置 | Smart+ 推广系列、推广系列类型、选择小程序、商品数据源、关联商品投放、付费模板组、免费广告策略模板、单日预算额度、广告系列状态 |
| 广告组设置 | 优化目标、选择应用内事件、价值事件、目标 ROAS、竞价策略、出价、事件统计方式、版位、指定版位、定向包 |
联动：优化目标候选随推广系列类型收窄；竞价策略候选随优化目标收窄；选择小程序、商品数据源、付费模板组、免费广告策略模板仅在推广系列类型为 TikTok Minis 时出现；关联商品投放仅在商品数据源开启时出现；指定版位仅在版位为指定版位时出现；定向包在商品数据源开启时隐藏。
TikTok Minis 另有三条强制：版位强制为指定版位且仅可勾选 TikTok、屏蔽 Pangle、回传模板隐藏。

#### TikTok / 销量
投放形态同 Meta / 销量。广告系列设置为 Smart+ 推广系列、商品数据源、关联商品投放、单日预算额度、广告系列状态。广告组设置为优化目标、优化事件、目标 ROAS、竞价策略、出价、事件统计方式、版位、指定版位、定向包。广告设置为行动号召、用户评论、视频下载、视频共享。

#### 商品对齐
TikTok 商品数据源开启后，可从「关联商品投放」打开商品对齐抽屉。抽屉为三栏：账户与商品库、商品列表、已选与短剧映射。支持自动匹配与手动指定。数据源关闭时清空已建立的映射。

#### 追踪配置
回传模板来源与投放策略、业务类型双向绑定，两者共同过滤可选的模板清单。像素 ID 仅在销量类目标下出现，选项按媒体过滤。H5 链接取投放策略为 H5 的监控链接清单。

### 4. 短剧包
圈剧方式二选一。
| 方式 | 行为 |
|------|------|
| 按规则 | 维度为语言、指标筛选、时间窗口、上线时间、初次投放时间 |
| 手选清单 | 须选定具体短剧列表 |
按规则时**仅允许筛选最近 30 天内有消耗的短剧**，更早的短剧不在候选范围内。
| 维度 | 说明 |
|------|------|
| 语言 | 下拉多选，不选即不限语种 |
| 指标筛选 | 条件行 + 条件关系，可加多行 |
| 时间窗口 | 当日 / 近三日 / 近七日 / 累计四选一，默认近七日，对全部指标条件统一生效。仅在有指标条件时出现 |
| 上线时间 | 起止日期区间，留空即不限 |
| 初次投放时间 | 起止日期区间，留空即不限 |
指标条件行为「指标 + 运算符 + 值」，运算符为大于等于、大于、等于、小于、小于等于、介于，百分比指标带百分号后缀、金额指标带美元后缀。行数达到 2 行及以上时出现条件关系，可选且或或，默认且。同一指标不可重复选择。
指标名单共 16 项：CAC、D0 归因收入、D0 充值用户数、当日纯新增用户数、当日回流用户数、广告 ARPU、Arppu、人均广告曝光、充值人数、收入、纯空耗金额、充值次数、订阅率、新增用户付费率、退订率、订阅成本。
定向包分配按圈剧语言分行，每行同时容纳 Meta 与 TikTok 两个定向包，取值可为空，为空表示该媒体不投该语种。语种不限时按全部语种逐行列出。定向包自身不带语种属性，候选不按语言过滤，仅按媒体过滤。
排期为包级单个时间输入，留空表示即刻投放。
手选清单模式下，短剧已逐部指定，定向包分配与排期两项均不展示；分行语言由已选短剧的语言派生，随短剧增减实时刷新。

### 5. 素材包
| 字段 | 必填 | 说明 |
|------|------|------|
| 素材选择方式 | 是 | 筛选 / 指定素材 |
| 素材创建时间 | 是 | 筛选模式下出现 |
| 素材数据筛选 | 是 | 不限 / 自定义 |
| 业务类型 | 否 | 筛选素材在免费 / 付费场景下的消耗数据，与账户包的业务类型不同义 |
| 筛选媒体 | 否 | 素材所属媒体 |
| 素材数量 | 是 | 不限 / 自定义 |
| 素材投放时间 | 否 | |
| 筛选条件 | 否 | 自定义筛选时必填，条件行不可重复 |
| 按指标排序 | 否 | |
| 素材单轮投放次数 | 否 | 不限 / 自定义。自定义时为单轮最多进入几个系列，跨账户合计 |
| 素材累计投放次数 | 否 | 不限 / 自定义。自定义时为累计投满即退出，跨轮跨账户 |
| 广告数 | 否 | 不填写默认 30 |
选择指定素材时，改为维护已选素材清单，随包保存并以标签呈现。

### 6. 定向包
字段按媒体分裂，切换媒体时整组替换。
| 媒体 | 字段 |
|------|------|
| Meta | 定向包名称、媒体、年龄（起止两个下拉）、性别、语言、移动设备、仅在连接 Wi-Fi 时、地区定向信息 |
| TikTok | 定向包名称、媒体、性别、年龄、语言、操作系统、网络环境、地区定向信息 |
表单按「定向信息 / 地区定向信息」分组面板呈现。地区定向信息为左右两栏：左栏是可搜索的地区列表，行内提供定向与排除两个操作，末列为复选框加全选；右栏为已选区，按定向与排除两个页签分列，可逐项移除。上方提供已选、批量导入、清除三个操作。
---
## 五、任务表单
### 1. 结构
```
基本信息：任务名称 · 状态 · 生效时间(UTC+8) · 执行频率
执行单元：媒体行列表（媒体 · 投放策略包 · 账户包 · 移除）+ 添加媒体
规则引用：短剧包（多选）· 素材包（多选）· 去重规则说明
运行约束：结束运行条件 · 单轮单短剧计划创建上限 · 每轮账户数
底部操作栏：取消 · 确定
```

### 2. 基本信息
| 字段 | 必填 | 说明 |
|------|------|------|
| 任务名称 | 是 | 最长 50 字 |
| 状态 | 是 | 启用 / 停用 |
| 生效时间 | 是 | 时间段，可添加多个范围，默认 00:00:00 至 23:59:59 |
| 执行频率 | 是 | 间隔若干分钟执行一次 |

### 3. 执行单元
媒体行可增删，首行不可删。每行选定媒体后，投放策略包与账户包两个下拉按该行媒体过滤，仅列出启用状态的同媒体包；媒体未选时两者置灰。
媒体行的媒体不可重复，同一媒体只能配置一行。
列表下方实时显示执行单元总数及拆解：`当前共 N 个执行单元（M 个媒体 × K 个短剧包 × L 个素材包）`。三个维度未填齐时不显示。

### 4. 规则引用
短剧包与素材包为多选，收起时显示已选包名，展开为可勾选列表，点击空白处关闭。单选时行为与多选一致。
下方常驻去重规则说明：同一媒体 + 同一账户包下，被多个包命中的短剧或素材只进池一次，配置以先选中的包为准；不同媒体各自建池，互不去重。

### 5. 运行约束
| 字段 | 说明 |
|------|------|
| 结束运行条件 | 两行固定条件：单账户单短剧在投总系列数、账户数，各带运算符与数值 |
| 单轮单短剧计划创建上限 | 兜底上限。不填即系列数仅由素材预算与素材池共同决定，不额外封顶；填 N 则本轮该短剧最多建 N 个系列 |
| 每轮账户数 | 风控开关。本轮最多允许几个账户参与建系列，控制单轮起量规模。不决定素材分给谁。清空即使用账户包全部账户，默认 5 |
后两项标签旁挂问号图标，鼠标悬停展示说明。每轮账户数旁显示各账户包的账户数，多包时取最小值作为上限。
---
## 六、校验
### 1. 任务保存校验
| # | 校验 | 提示 |
|------|------|------|
| 1 | 任务名称必填 | 请输入任务名称 |
| 2 | 执行频率必填 | 请输入执行频率 |
| 3 | 至少一个完整媒体行 | 请至少填写一个完整的执行单元 |
| 4 | 媒体行内媒体不可重复 | 同一个媒体只能配置一行 |
| 5 | 短剧包至少选一个 | 请选择短剧包 |
| 6 | 素材包至少选一个 | 请选择素材包 |
| 7 | 引用的包不得为停用 | 引用的包已停用，执行时将告警并跳过本轮 |
| 8 | 素材包中存在指定素材时，短剧包中必须存在手选清单 | 指定素材需配合手选短剧清单使用 |
| 9 | 账户包内账户数非空 | 当前账户包内无账户 |
| 10 | 每轮账户数不得超过各账户包中最少的账户数 | 每轮账户数不能超过各账户包中最少的账户数 |
| 11 | 结束运行条件数值必填 | 请填写结束运行条件的数值 |
| 12 | 单轮单短剧计划创建上限小于 1 且非空时拦截 | 计划创建上限不能小于 1（不填=不限） |
| 13 | 每轮账户数小于 1 且非空时拦截 | 每轮账户数不能小于 1 |

### 2. 包保存校验
| 包 | 校验 |
|------|------|
| 账户包 | 名称必填、媒体必选 |
| 投放策略包 | 名称必填、媒体与广告目标必选，各分组内必填项校验 |
| 定向包 | 名称必填、媒体必选；Meta 下地区定向信息必填且至少选择一个地区；年龄区间起始值不得大于结束值 |
| 短剧包 | 手选清单模式下至少添加一部短剧；按规则模式下至少设置一个圈剧规则维度 |
| 素材包 | 指定素材模式下至少选择 1 个素材；自定义筛选时至少输入一个条件 |

### 3. 连带校验
被任一任务引用的包不可删除；被短剧包引用的定向包不可删除。保存包的编辑内容时，若该包被多个任务引用，提示影响面，说明保存后这些任务下次执行时生效。
---
## 七、待确认
| # | 事项 | 说明 |
|------|------|------|
| 1 | 每轮账户数的执行语义 | 原型按「每个执行单元各自适用、取各自账户包的账户」实现，后台是否按此执行需确认 |
| 2 | 执行单元的去重落点 | 原型按「同一媒体 + 同一账户包下短剧与素材只进池一次、以首个包为准」说明，后台是否按此实现需确认 |
| 3 | 渠道选项来源 | 渠道名称与渠道号合并为一个下拉，真实选项是固定枚举还是来自渠道管理配置，需确认 |
| 4 | 地区列表全选与批量导入 | 末列行复选框与全选在参考界面中未体现选中后的落点，批量导入按粘贴文本解析，真实语义待确认 |
| 5 | 商品对齐的落库形态 | 自动匹配结果与手工指定的商品是否随包落库，还是每次执行重算，待确认 |
| 6 | 执行单元的历史留存 | 子行展示本轮还是最近一轮，跨轮次历史是否保留，取决于后台是否落库单元级执行记录 |
| 7 | 单元总数上限 | 是否需要为执行单元总数设硬上限，防止误操作放大投放规模，待运营确认 |
| 8 | 任务行状态归并 | 任一单元失败则任务标记异常已定，部分成功是否需独立状态待确认 |